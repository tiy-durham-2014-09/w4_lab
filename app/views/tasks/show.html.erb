<h1><%= @list.title %></h1>

<form>
  <% Task.all.each do |task| %> <%# look at DOM ID to ID each task, call params[:complete] %>
      <div><input value="<%= task.title %>"  name="complete[]" type="checkbox"><%= task.title %></div>
  <% end %>
</form>


<%= link_to 'Add New Task', new_list_task_path %>