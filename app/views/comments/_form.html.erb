<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <div class="large-6 columns">
    <div class="field">
    <%= f.label :user_id,nil, class: 'comment_label' %><br>
    <%= f.collection_select :user_id, User.all, :id, :name, :include_blank => true %>
  </div>
</div>
  <!-- <div class="large-4 columns">
  <div class="field">
    <%= f.label :user,nil, class: 'small_label' %><br>
    <%= f.text_field :user %>
  </div>
</div> -->
  <div class="large-6 columns">

  <div class="field">
    <%= f.label :post ,nil, class: 'comment_label' %><br>
    <%= f.text_field :post %>
  </div>
</div>
<div class="row">
  <div class="large-12 columns">
  <div class="field">
    <%= f.label :body,nil, class: 'comment_label' %><br>
    <%= f.text_area :body, class: 'text_field' %>
  </div>
</div>
</div>
  <div class="actions">
    <%= f.button :submit, class: "radius small" %>

  </div>
<% end %>
