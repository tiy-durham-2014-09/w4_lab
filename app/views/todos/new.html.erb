<h1>New Todo</h1>

<%= simple_form_for @todo do |f| %>
<div class="large-12 columns">
  <%= f.input :title, label: 'Add a new todo to your list' %>
</div>
  <%= f.input :date_due, inline_label: 'Enter the due date' %>
  <%= f.button :submit %>
<% end %>

<%= link_to 'Back', todos_path %>
