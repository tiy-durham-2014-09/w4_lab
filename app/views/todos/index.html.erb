<h1>Listing All Todos</h1>

<table>
  <thead>
    <tr>
      <th>List</th>
      <th>Todo</th>
      <th>Note</th>
      <th>Due date</th>
      <th colspan="1"></th>
    </tr>
  </thead>

  <tbody>
    <% @todos.each do |todo| %>
      <tr>
        <% unless todo.done %>

          <td><%= link_to todo.list.name, list_path(todo.list.id) %></td>
          <td><%= link_to todo.name, todo_path(todo.id) %></td>
          <td><%= todo.note %></td>
          <td><%= todo.due_date %></td>
          <td><%= link_to 'Edit', edit_todo_path(todo) %></td>
        <% end %>
        </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Todo', new_todo_path %>
